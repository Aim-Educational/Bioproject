#include "ActivityLog.h"

ActivityLogEntry standardLogEntry(String errorName)
{
    switch(errorName)
    {
        case "asdasd": break;
        default: break; // Will finish in a second.
    }

    return ActivityLogEntry();
}
